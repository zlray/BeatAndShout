<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.xqlh.beatandshout">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <meta-data -->
        <!-- android:name="design_width_in_dp" -->
        <!-- android:value="1920"/> -->
        <!-- <meta-data -->
        <!-- android:name="design_height_in_dp" -->
        <!-- android:value="1200"/> -->

        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".login.LoginActivity">
        </activity>
        <activity android:name=".brainwave.BrainWaveActivity">
        </activity>
        <activity android:name=".account.AccountActivity">
        </activity>
        <activity android:name=".login.UpdatePasswordActivity">
        </activity>
        <activity android:name=".account.CreateManagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".account.CreateUserActivity">
        </activity>
        <activity android:name=".account.CreateUserBatchActivity">
        </activity>
        <activity android:name=".realTimeMonitoring.MonitoringActivity">
        </activity>
    </application>

</manifest>